package no.hist.aitel.team12.app;
/*
import java.awt.image.BufferedImage;
import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.io.File;
import java.io.IOException;
import java.io.InputStream;

import javax.imageio.ImageIO;

import org.jfree.chart.ChartFactory;
import org.jfree.chart.ChartFrame;
import org.jfree.chart.ChartUtilities;
import org.jfree.chart.JFreeChart;
import org.jfree.chart.plot.CategoryPlot;
import org.jfree.chart.plot.PlotOrientation;
import org.jfree.chart.renderer.category.BarRenderer;
import org.jfree.data.jdbc.JDBCCategoryDataset;
*/

public class FinanceChart {

/*	
	public FinanceChart() {
	    	//https://www.youtube.com/watch?v=z0fLprufs0o
	    	String query = "Select date, revenue from Revenue";
	    	JDBCCategoryDataset dataset = new JDBCCategoryDataset(javaconnect.ConnecrDb(),query);
	    	JFreeChart chart = ChartFactory.createBarChart("Revenue", "Date","Revenue" , dataset, PlotOrientation.VERTICAL,false,true,true);
	    	BarRenderer renderer = null;
	    	CategoryPlot plot=null;
	    	renderer=new BarRenderer();
	    	ChartFrame frame = new ChartFrame("Revenue chart",chart);
	    	frame.setVisible(true);
	    	frame.setSize(400,650);
	    	
	    	// Must write code for saving chart to image, so pdfGenerator can paste it to a paragraph
	    	// http://stackoverflow.com/questions/5272966/jfreechart-image
	    	BufferedImage objBufferedImage=chart.createBufferedImage(600,800);
	    	ByteArrayOutputStream bas = new ByteArrayOutputStream();
	    		try{
	    			ImageIO.write(objBufferedImage, "png", bas);
	    		}catch (IOException e){
	    			e.printStackTrace();
	    		}
	    	byte[] byteArray = bas.toByteArray();
	    	
	    	InputStream in = new ByteArrayInputStream(obj);
	    	BufferedImage image = ImageIO.read(in);
	    	File outputfile = new File("image.png");
	    	ImageIO.write(image,"png",outputfile);
	    	
	    	// OR
	    	
	    	ChartUtilities.writeChartAsPNG(outputstream,getDataset(),width,height);
*/
	    }
 


	      

